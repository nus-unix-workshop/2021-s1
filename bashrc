You should add this alias 

```bash
alias rm="rm -i"
```

to your `.bashrc` (Google to find out how).  Other useful aliases you should put into `.bashrc` to avoid accidentally overwriting existing files are:

```bash
alias mv="mv -i"
alias cp="cp -i"
```
